#include "main.h"
/**
 * print_rev - main function
 *
 * @s: parameter
 *
 */
void print_rev(char *s)
{
	int n;

	for (n = 0; s[n] != '\0'; n++)

	while (n < 0)
	{
		_putchar(s[--n]);
	}
	_putchar(10)
}
