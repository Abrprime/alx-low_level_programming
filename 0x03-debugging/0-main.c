gcc -E $CFILE -o c
gcc -c $CFILE
gcc -S $CFILE
gcc $CFILE -o cisfun
#include <stdio.h>
/**
* main - entry point
*
* Return: 0 number
*/
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
